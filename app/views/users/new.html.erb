<div class="form-space">
  <h3 class="title">ユーザー登録</h3>
  <%= form_for(@user) do |f| %>
      <div class="form-border">
      <div class="form-box">
        <label class="form-label">お名前</label>
        <%= f.text_field :name, {class: 'form'} %>
      </div>
      <div class="form-box">
        <label class="form-label">メールアドレス(半角英数字)</label>
        <%= f.email_field :email, {class: 'form'} %>
      </div>
      <div class="form-box">
        <label class="form-label">MetaMaskアドレス</label>
        <%= f.text_field :meta_address, {class: 'form'} %>
      </div>
      <div class="form-box">
        <label class="form-label">パスワード</label>
        <%= f.password_field :password, {class: 'form'} %>
      </div>
      <%= f.submit '登録', {class: 'button'} %>
      </div>
  <% end %>

  <p>MetaMaskの登録がお済みでない方は,<br>以下をお読みの上そちらの登録を宜しくお願いします</p>
  <%= link_to '取引の前に', explanation_path  %>
</div>